import styled from 'styled-components'

export const CarousellContainer = styled('div')`
	padding: 20px;
	display: flex;
	justify-content: center;
	gap: 20px;
	overflow: hidden;
`

export const CarousellImage = styled('img')`
	width: 200px;
	border-radius: 10px;
	border: 1px solid rgba(128, 128, 128, 0.1);
	box-shadow: 0px 5px 15px rgba(128, 128, 128, 0.05);
`
